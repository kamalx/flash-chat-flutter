name: flash_chat
description: Mulberry Apps Flash Chat

version: 0.0.1

environment:
  sdk: ">=2.1.0<3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animated_text_kit: ^2.5.4
  cloud_firestore: ^0.14.3
  cupertino_icons: ^0.1.2
  firebase_auth: ^0.18.3
  firebase_core: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
